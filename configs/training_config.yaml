model:
  architecture: cnn
  num_classes: 10

data:
  data_dir: data

training:
  batch_size: 64
  num_workers: 2
  learning_rate: 0.001
  epochs: 20
  early_stopping_patience: 3

output:
  checkpoint_dir: checkpoints
  model_name: cifar10_cnn.pt
